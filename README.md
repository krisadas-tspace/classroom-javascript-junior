# 📉 The Flaky Dashboard Challenge

ยินดีต้อนรับสู่โจทย์วัดทักษะ Backend/Node.js
**กติกา:** อนุญาตให้ใช้ AI (ChatGPT/Copilot) ได้เต็มที่ แต่คุณต้องเข้าใจสิ่งที่เขียน

### ⚠️ ปัญหา
ไฟล์ `src/dashboard.js` ทำหน้าที่ดึงข้อมูลจาก API 3 ตัวมาแสดงผล แต่ API (`src/mock_api.js`) นั้นมีปัญหา:
1.  **ช้า (Slow):** ตอบกลับช้า 0.5 - 1.5 วินาที
2.  **ไม่เสถียร (Unstable):** มีโอกาส 30% ที่จะ Error 500
3.  **จำกัดจำนวน (Rate Limit):** ถ้ายิงถี่เกินไปจะ Error 429

โค้ดปัจจุบันทำงานแบบ **Sequential** (ทำทีละอย่าง) และไม่มี **Retry** ทำให้ User เห็นหน้าจอขาวบ่อยมาก

### ✅ ภารกิจของคุณ
แก้ไขไฟล์ `src/dashboard.js` และ `src/util.js` ให้บรรลุเป้าหมาย:

1.  **Speed:** เปลี่ยนการทำงานเป็น **Parallel** (ทำพร้อมกัน) เพื่อลดเวลารวม
2.  **Stability:** ต้องมีระบบ **Retry Mechanism** 
    - ถ้าเจอ Error 500 ให้ลองเรียกใหม่ (สูงสุด 3 ครั้ง)
    - ถ้าเจอ Error 429 (Rate Limit) ให้รอสักพักแล้วลองใหม่ (Backoff)
3.  **Result:** ฟังก์ชัน `loadDashboard()` จะต้อง return ข้อมูลให้ครบทั้ง 3 ส่วนเสมอ (ยกเว้น API พังเกินเยียวยาจริงๆ)

### 🧪 การส่งงาน (Blind Test)
1.  เขียน Unit Test ของคุณเองในโฟลเดอร์ `tests/` เพื่อทดสอบความถูกต้อง
2.  **ระบบตรวจอัตโนมัติถูกปิดไว้** (คุณจะไม่รู้ผลสอบตอน Push)
3.  ให้มั่นใจก่อนส่ง เราจะตรวจจาก Commit สุดท้ายเท่านั้น
