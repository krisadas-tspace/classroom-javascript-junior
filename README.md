# 🛒 JS Refactoring Challenge (AI Allowed)

ยินดีต้อนรับสู่การทดสอบทักษะ Coding โจทย์นี้อนุญาตให้คุณใช้ **ChatGPT, Claude หรือ Copilot** ได้เต็มที่
เราไม่ได้วัดผลที่ "ความจำ" แต่เราวัดผลที่ **"ความถูกต้องและการตรวจสอบ (Verification)"**

### ⚠️ ปัญหาปัจจุบัน
ไฟล์ `src/cart.js` เป็นโค้ด legacy ที่เขียนไว้แย่มาก:
- ชื่อตัวแปรอ่านไม่รู้เรื่อง (`n`, `p`, `q`)
- มีบั๊กเรื่องการคำนวณเงิน (Floating point precision)
- ไม่มีการป้องกันข้อมูลผิดพลาด (Validation)

### ✅ สิ่งที่คุณต้องทำ
แก้ไขไฟล์ `src/cart.js` ให้เป็น **Clean Code** โดยมีข้อกำหนดดังนี้:

1. **Refactor Names:** เปลี่ยนชื่อ method ให้สื่อความหมาย (Interface Requirement):
   - `add(n, p, q)`  ➡️ เปลี่ยนเป็น `addItem(name, price, quantity)`
   - `calculate()`   ➡️ เปลี่ยนเป็น `getTotal()`
   - `discount(pct)` ➡️ เปลี่ยนเป็น `applyDiscount(percent)`

2. **Fix Bugs:**
   - แก้ปัญหาการบวกเลขทศนิยมใน JavaScript (เช่น 0.1 + 0.2 ต้องได้ 0.3)
   - ป้องกันไม่ให้ใส่ `price` หรือ `quantity` ที่ติดลบ (ให้ `throw Error`)

3. **New Feature:**
   - เพิ่ม method `removeItem(name)` เพื่อลบสินค้าออกจากตะกร้า

4. **Test:**
   - เขียน Unit Test ในโฟลเดอร์ `tests/` เพื่อพิสูจน์ว่าโค้ดของคุณทำงานถูกต้อง

### 🚀 การส่งงาน
เมื่อคุณมั่นใจแล้ว ให้ Push code ขึ้นมา ระบบจะทำการรัน **Automated Grading** เพื่อตรวจความถูกต้อง
